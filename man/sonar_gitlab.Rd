% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sonar_gitlab.R
\name{sonar_gitlab}
\alias{sonar_gitlab}
\title{Run rsonar in GitLab CI (Analyse or Fix)}
\usage{
sonar_gitlab(command = c("both", "analyse", "fix"), path = ".", ...)
}
\arguments{
\item{command}{Character. What to do in GitLab CI.
\itemize{
  \item \code{"analyse"} : run \code{\link[=sonar_analyse]{sonar_analyse()}} + \code{\link[=quality_gate]{quality_gate()}} + \code{\link[=sonar_report]{sonar_report()}}
  \item \code{"fix"}     : run \code{\link[=sonar_fix]{sonar_fix()}} + commit + push + create Merge Request
  \item \code{"both"}    : run analyse first, then fix automatically if issues detected
}
Default \code{"both"}.}

\item{path}{Path to the R project directory. Default \code{"."}.}

\item{...}{Additional arguments passed to the underlying functions.}
}
\value{
Invisibly, the result object (analyse result, fix result, or both).
}
\description{
Generic GitLab CI integration function. Provides a single entry point for running rsonar in GitLab CI pipelines with a simple command parameter.
}
\section{GitLab CI environment}{

The function auto-detects \code{CI_SERVER_URL}, \code{CI_PROJECT_ID}, \code{CI_COMMIT_BRANCH}, \code{CI_DEFAULT_BRANCH} from GitLab CI. No manual configuration needed.
}

\section{Token authentication}{

Uses \code{GITLAB_TOKEN} environment variable for Merge Request creation. Falls back to \code{CI_JOB_TOKEN}.
}

\examples{
\dontrun{
# Full analysis in GitLab CI
sonar_gitlab("analyse")

# Auto-fix and create Merge Request
sonar_gitlab("fix")

# Both: analyse + fix if needed
sonar_gitlab("both")
}
}
\seealso{
\code{\link[=sonar_analyse]{sonar_analyse()}}, \code{\link[=sonar_fix]{sonar_fix()}}
}