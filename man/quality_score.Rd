% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{quality_score}
\alias{quality_score}
\title{Quick Quality Score for IDE Usage}
\usage{
quality_score(
  x = ".",
  include_coverage = FALSE,
  include_goodpractice = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{x}{A path to analyze (character) or an \code{rsonar_result} object.
Default \code{"."}.}

\item{include_coverage}{Logical. Include coverage in quick analysis when
\code{x} is a path. Default \code{FALSE} for speed.}

\item{include_goodpractice}{Logical. Include goodpractice checks in quick
analysis when \code{x} is a path. Default \code{FALSE} for speed.}

\item{verbose}{Logical. Show progress and summary in console.
Default \code{TRUE}.}
}
\value{
An object of class \code{rsonar_score} with fields:
\code{score} (0-100), \code{rating} (A-E), \code{ratio}, \code{path}, \code{timestamp}.
}
\description{
Computes a quick quality percentage score for a project, designed for
fast feedback directly in the IDE without any CI platform.
}
\details{
You can pass either:
\itemize{
\item a path to an R project/package, or
\item an existing \code{rsonar_result} object.
}

The score is derived from the technical debt ratio:
\code{score = 100 * (1 - min(1, debt_ratio))}.
}
\examples{
\dontrun{
# Fast local check directly in IDE
quality_score(".")

# Reuse an existing analysis
res <- sonar_analyse(".")
quality_score(res)
}

}
