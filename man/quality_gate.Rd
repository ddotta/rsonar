% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quality_gate.R
\name{quality_gate}
\alias{quality_gate}
\title{Define and Check a Quality Gate}
\usage{
quality_gate(
  x,
  coverage_min = 80,
  lint_errors_max = 0,
  lint_warnings_max = Inf,
  style_issues_max = 0,
  gp_fails_max = Inf,
  rating_min = "C",
  fail_on_error = FALSE
)
}
\arguments{
\item{x}{An \code{rsonar_result} object returned by \code{\link[=sonar_analyse]{sonar_analyse()}}.}

\item{coverage_min}{Minimum required coverage in \%. Default \code{80}. \code{NULL}
to disable this threshold.}

\item{lint_errors_max}{Maximum number of tolerated lint errors. Default \code{0}.}

\item{lint_warnings_max}{Maximum number of tolerated lint warnings. Default \code{Inf}.}

\item{style_issues_max}{Maximum number of improperly formatted files. Default \code{0}.}

\item{gp_fails_max}{Maximum number of goodpractice failures. Default \code{Inf}.}

\item{rating_min}{Minimum required SQALE rating (\code{"A"} to \code{"E"}). Default \code{"C"}.}

\item{fail_on_error}{If \code{TRUE}, stops the R process with \code{quit(status = 1)}
when the gate fails. Useful in CI. Default \code{FALSE}.}
}
\value{
An \code{rsonar_gate} object (list) with:
\describe{
\item{\code{passed}}{\code{TRUE} if all thresholds are met}
\item{\code{checks}}{Detailed data frame of each check}
}
}
\description{
Reproduces the behavior of the SonarQube \strong{Quality Gate}:
defines quality thresholds and returns \code{TRUE}/\code{FALSE} based on whether
the project meets them. In CI, the R process exit code can be set to 1
to block the pipeline on failure.
}
\examples{
\dontrun{
res <- sonar_analyse(".")

# Strict gate: 0 errors, 80\% coverage
gate <- quality_gate(res, coverage_min = 80, lint_errors_max = 0)
print(gate)

# In CI: exit with code 1 on failure
quality_gate(res, coverage_min = 80, fail_on_error = TRUE)
}

}
