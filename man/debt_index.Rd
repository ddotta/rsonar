% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/debt.R
\name{debt_index}
\alias{debt_index}
\title{Estimate Technical Debt}
\usage{
debt_index(
  x,
  cost_lint_error = 30,
  cost_lint_warning = 10,
  cost_lint_style = 2,
  cost_style = 5,
  cost_gp = 20,
  coverage_target = 80,
  cost_coverage_point = 5
)
}
\arguments{
\item{x}{An \code{rsonar_result} object returned by \code{\link[=sonar_analyse]{sonar_analyse()}}.}

\item{cost_lint_error}{Cost in minutes per lint issue of type \code{error}.
Default \code{30}.}

\item{cost_lint_warning}{Cost in minutes per lint warning. Default \code{10}.}

\item{cost_lint_style}{Cost in minutes per lint style violation. Default \code{2}.}

\item{cost_style}{Cost in minutes per improperly formatted file (styler).
Default \code{5}.}

\item{cost_gp}{Cost in minutes per goodpractice failure. Default \code{20}.}

\item{coverage_target}{Target coverage in \%. Default \code{80}.}

\item{cost_coverage_point}{Cost in minutes per missing coverage point
below the target. Default \code{5}.}
}
\value{
An \code{rsonar_debt} object (list) containing:
\describe{
\item{\code{minutes}}{Total estimated debt in minutes}
\item{\code{hours}}{Total estimated debt in hours}
\item{\code{rating}}{SQALE rating: "A", "B", "C", "D" or "E"}
\item{\code{breakdown}}{Data frame with details by category}
}
}
\description{
Computes a technical debt index inspired by the SQALE model used by
SonarQube. The index is expressed in estimated remediation minutes and as
a rating from A (excellent) to E (critical).
}
\examples{
\dontrun{
res <- sonar_analyse(".")
d <- debt_index(res)
print(d)
# Total debt: 2.5h — Rating: B
}

}
