% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autofix.R
\name{sonar_autofix}
\alias{sonar_autofix}
\title{Auto-Fix and Create Merge/Pull Request (GitLab or GitHub)}
\usage{
sonar_autofix(
  provider = c("auto", "gitlab", "github"),
  path = ".",
  fixes = "all",
  formatter = c("styler", "air"),
  branch = "auto/rsonar-fix",
  target_branch = NULL,
  commit_message = "style: automatic fixes by rsonar",
  mr_title = "Automatic fixes generated by rsonar",
  mr_description = NULL,
  dry_run = FALSE,
  token = "",
  verbose = interactive()
)
}
\arguments{
\item{provider}{Character. Git platform to use.}

\item{path}{Path to the R project directory. Default \code{"."}.}

\item{fixes}{Fix categories to apply. Default \code{"all"}.}

\item{formatter}{Formatter to use. Default \code{c("styler", "air")}.}

\item{branch}{Name of the branch to create for fixes. Default \code{"auto/rsonar-fix"}.}

\item{target_branch}{Target branch for the MR/PR. Auto-detected from CI.}

\item{commit_message}{Git commit message.}

\item{mr_title}{Title of the MR/PR.}

\item{mr_description}{Description of the MR/PR. If \code{NULL}, a default
description is generated including the pipeline ID. Use this to
include quality metrics (coverage, goodpractice issues, etc.) in the
merge request body.}

\item{dry_run}{Logical. If \code{TRUE}, only simulate without modifying.}

\item{token}{Authentication token. Auto-detected if empty.}

\item{verbose}{Logical. Show progress messages.}
}
\value{
Invisibly, a list with results.
}
\description{
Generic function for automatic code fixes with Git platform integration.
Runs \code{\link[=sonar_fix]{sonar_fix()}} to correct code issues, then commits changes and
creates a Merge Request (GitLab) or Pull Request (GitHub).
}
