% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hotspots.R
\name{sonar_hotspots}
\alias{sonar_hotspots}
\title{Rank Files by Technical Debt (Hotspots)}
\usage{
sonar_hotspots(
  x,
  n = 10,
  cost_lint_error = 30,
  cost_lint_warning = 10,
  cost_lint_style = 2,
  cost_style = 5
)
}
\arguments{
\item{x}{An \code{rsonar_result} object returned by \code{\link[=sonar_analyse]{sonar_analyse()}}.}

\item{n}{Maximum number of files to return. Default \code{10}.}

\item{cost_lint_error}{Cost in minutes per lint issue of type \code{error}.
Default \code{30}.}

\item{cost_lint_warning}{Cost in minutes per lint warning. Default \code{10}.}

\item{cost_lint_style}{Cost in minutes per lint style violation. Default \code{2}.}

\item{cost_style}{Cost in minutes per improperly formatted file (styler).
Default \code{5}.}
}
\value{
An \code{rsonar_hotspots} object (a data frame) with one row per file,
ordered by decreasing debt, containing the columns:
\describe{
\item{\code{rank}}{Rank position, \code{1} = highest debt}
\item{\code{file}}{File path, relative to the analyzed project}
\item{\code{lint_errors}, \code{lint_warnings}, \code{lint_style}}{Issue counts by
severity, from lintr}
\item{\code{style_issue}}{\code{TRUE} if the file needs re-formatting (styler)}
\item{\code{coverage_pct}}{Line coverage for the file, or \code{NA} if
coverage was not computed or the file has no coverage data}
\item{\code{debt_minutes}}{Estimated remediation effort, in minutes}
}
}
\description{
\code{debt_index()} and \code{quality_score()} give a single project-wide number,
but they don't tell you \strong{where} to start fixing things. \code{sonar_hotspots()}
fills that gap: it breaks the technical debt down \strong{per file} and ranks
files from the most to the least costly to remediate -- the R equivalent
of SonarQube's "Code Smells" / hotspots view, which developers use to
prioritize their first pull request after an audit.
}
\examples{
\dontrun{
res <- sonar_analyse(".")

# The 10 files that concentrate the most technical debt
hotspots <- sonar_hotspots(res)
print(hotspots)

# Only the top 3, with custom weights
sonar_hotspots(res, n = 3, cost_lint_error = 60)
}

}
\seealso{
\code{\link[=debt_index]{debt_index()}}, \code{\link[=sonar_analyse]{sonar_analyse()}}, \code{\link[=quality_gate]{quality_gate()}}
}
