% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse.R
\name{sonar_analyse}
\alias{sonar_analyse}
\title{Complete R Project Quality Analysis}
\usage{
sonar_analyse(
  path = ".",
  include_lint = TRUE,
  include_style = TRUE,
  include_coverage = fs::dir_exists(fs::path(path, "tests")),
  include_goodpractice = fs::file_exists(fs::path(path, "DESCRIPTION")),
  exclude_pattern = "(\\\\.git|\\\\.ci|renv|packrat|vendor|node_modules|_snaps)",
  lintr_config = NULL,
  verbose = TRUE
)
}
\arguments{
\item{path}{Path to the R project or package to analyze. Defaults to
the current directory.}

\item{include_lint}{Logical. Enable lintr static analysis. Default \code{TRUE}.}

\item{include_style}{Logical. Enable styler style checking.
Default \code{TRUE}.}

\item{include_coverage}{Logical. Enable covr coverage measurement.
Default \code{TRUE} if a \verb{tests/} directory exists.}

\item{include_goodpractice}{Logical. Enable goodpractice checks. Default
\code{TRUE} if a \code{DESCRIPTION} file exists.}

\item{exclude_pattern}{Regular expression to exclude files.
Default \code{"(\\\\.git|\\\\.ci|renv|packrat|vendor|node_modules|_snaps)"}.}

\item{lintr_config}{Path to a custom \code{.lintr} file. If \code{NULL},
rsonar automatically looks for \code{.lintr} in \code{path}.}

\item{verbose}{Logical. Show progress in the console. Default \code{TRUE}.}
}
\value{
An object of class \code{rsonar_result} containing:
\describe{
\item{\code{lint}}{List of lintr issues (class \code{lints})}
\item{\code{style}}{Data frame of files with style issues}
\item{\code{coverage}}{A covr object or \code{NULL}}
\item{\code{goodpractice}}{A goodpractice object or \code{NULL}}
\item{\code{metrics}}{Data frame of consolidated metrics}
\item{\code{debt}}{Technical debt estimate}
\item{\code{path}}{Analyzed path}
\item{\code{timestamp}}{Analysis date/time}
}
}
\description{
Main function of \code{rsonar}. Orchestrates static analysis (lintr),
style checking (styler), test coverage measurement (covr) and
packaging best practices (goodpractice), then returns an
\code{rsonar_result} object summarizing all results.
}
\examples{
\dontrun{
# Analyze the current package
res <- sonar_analyse(".")
print(res)

# Analyze without coverage (faster)
res <- sonar_analyse(".", include_coverage = FALSE)

# With a custom .lintr file
res <- sonar_analyse(".", lintr_config = "custom.lintr")
}

}
\seealso{
\code{\link[=sonar_report]{sonar_report()}}, \code{\link[=debt_index]{debt_index()}}, \code{\link[=quality_gate]{quality_gate()}}
}
