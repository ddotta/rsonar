# Print an rsonar_diff Object

Print an rsonar_diff Object

## Usage

``` r
# S3 method for class 'rsonar_diff'
print(x, ...)
```

## Arguments

- x:

  An `rsonar_diff` object.

- ...:

  Additional arguments (ignored).

## Value

`x` invisibly.
